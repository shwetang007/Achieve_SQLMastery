SELECT *
FROM users;
/* retrives alll the data
 from the users table.
 */
SELECT id
from users;
SELECT id,
    name
from users;
SELECT *
from users;
CREATE TABLE users (id INTEGER, name TEXT, age INTEGER);
INSERT into users (id, name, age)
values (1, 'John Doe', 21);
INSERT into users (id, name, age)
values (2, 1, 33);
SELECT *
FROM users;
