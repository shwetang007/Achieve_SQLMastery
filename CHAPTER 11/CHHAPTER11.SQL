CREATE TABLE USERS (
    ID INTEGER PRIMARY KEY,
    EMAIL TEXT,
    NAME TEXT,
    AGE INTEGER
);

CREATE TABLE EMAIL_IDX ON USERS(
    EMAIL
);

SELECT
    NAME
FROM
    SQLITE_MASTER
WHERE
    TYPE = 'index';

CREATE TABLE TRANSACTIONS (
    ID INTEGER PRIMARY KEY,
    USER_ID INTEGER,
    RECIPIENT_ID INTEGER,
    SENDER_ID INTEGER,
    AMOUNT INTEGER
);

CREATE INDEX USER_ID_IDX ON TRANSACTION(USER_ID, RECIPIENT ID);

SELECT
    TYPE,
    NAME,
    TBL_NAME
FROM
    SQLITE_MASTER
WHERE
    TYPE = 'index';
